@model Contracts.ViewModels.Schedule.ActivityByGroupEditViewModel
@await Html.PartialAsync("_editPartialByGroup.cshtml", Model)
<form asp-action="Delete" asp-route-id="@Model.Id" asp-route-name="@Model.ClassGroup"
    asp-route-timestamp="@Model.Timestamp">
    <div class="form-group">
        <input type="submit" value="Delete" class="btn btn-danger" />
    </div>
</form>
<div>
    <a asp-action="Index" asp-route-name="@Model.ClassGroup">Back to List</a>
</div>